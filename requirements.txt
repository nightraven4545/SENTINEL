# CPU-only torch wheels (Linux default is the ~2GB CUDA build — Sentinel's
# tiny autoencoder needs none of it; keeps Streamlit Cloud/Docker deploys lean)
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Core data ---
pandas
numpy

# --- Data ingestion (free, no paid keys) ---
yfinance          # market OHLCV (FRED/EDGAR loaders are future work — see README)

# --- Storage ---
duckdb            # local analytical warehouse

# --- Models / ML ---
scikit-learn      # IsolationForest anomaly baseline
torch             # PyTorch autoencoder
networkx          # correlation graph

# --- Serving ---
fastapi
uvicorn[standard]
pydantic

# --- Dashboard & charts ---
streamlit
plotly

# --- Agent / reports ---
anthropic         # LLM memo writer (optional; falls back to template if no key)
reportlab         # memo -> PDF

# --- Notebooks (headless execution + static chart export for GitHub) ---
ipykernel
nbclient
nbformat
kaleido

# --- Config & tests ---
python-dotenv
pytest
