services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false   # runs without a key (template memo mode)
    volumes:
      - sentinel-data:/app/data

volumes:
  sentinel-data:
